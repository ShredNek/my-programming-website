import React from "react";
import { render } from "@testing-library/react";
import { beforeEach, describe } from "vitest";
import _WipButton from "../src/components/_WipButton";

describe("Button renders text and self-focus correctly", () => {
  beforeEach(() => {
    render(<_WipButton />);
  });

  it();
});
